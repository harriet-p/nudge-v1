/** @deprecated Import from `src/nudges/policies` instead. */
export {
  COMPLETION_COOLDOWN_MINUTES,
  DISMISS_BACKOFF_MULTIPLIER,
  getEffectiveIntervalMs,
  isWithinQuietHours,
  MAX_CHECK_INTERVAL_MS,
  MIN_CHECK_INTERVAL_MS,
} from '../nudges/policies'
