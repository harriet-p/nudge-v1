export const BUTTONS = {
  accept: "Let's go",
  dismiss: 'Later',
} as const

export const GREETING = {
  prefix: 'Hi',
} as const

/** Idle startup + affection response (Figma startup sequence). */
export const AFFECTION = {
  connectLabel: 'Hi Baby',
  loveLabel: 'I love you',
  responseDialogue: 'I miss you.',
  responseLabel: 'me too',
} as const
